# Shared zoxide configuration
# Used by both NixOS and Home Manager modules
# See: config/nixos/modules/applications/zoxide.nix (NixOS)
# See: config/home-manager/modules/applications/zoxide.nix (Home Manager)
{
  # Zoxide has minimal config - just enable
  # Shell integration is handled automatically by programs.zoxide
}
